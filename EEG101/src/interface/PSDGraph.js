// PSDGraph.
// A single module for PSD graph view so that React Native >.049 doesn't complain
import { requireNativeComponent } from "react-native";

const PSDGraph = requireNativeComponent("PSD_GRAPH", null);

export default PSDGraph;
